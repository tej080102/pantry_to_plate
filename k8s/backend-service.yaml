apiVersion: v1
kind: Service
metadata:
  name: pantry-backend-svc
  namespace: pantry
spec:
  selector:
    app: pantry-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
